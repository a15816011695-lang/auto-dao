# 源码阅读顺序 (File Reading Order)

<!-- 可选产物：仅在 GitHub 项目学习模式中，用户表示"想读源码"或"想了解实现"时按需生成 -->

**项目**: {project_name}
**仓库**: {repo_url}
**生成时间**: {timestamp}

---

## 推荐阅读顺序

按照"入口 → 核心 → 辅助"的原则，推荐以下阅读路径：

### 第一轮：理解入口和配置

| 顺序 | 文件 | 阅读重点 | 预计时间 |
|------|------|---------|---------|
| 1 | `{entry_file}` | 程序入口，理解启动流程 | {time} min |
| 2 | `{config_file}` | 配置项，理解可调参数 | {time} min |

### 第二轮：核心逻辑

| 顺序 | 文件 | 阅读重点 | 预计时间 |
|------|------|---------|---------|
| 3 | `{core_file_1}` | 核心算法/业务逻辑 | {time} min |
| 4 | `{core_file_2}` | 数据处理/模型层 | {time} min |

### 第三轮：辅助与扩展

| 顺序 | 文件 | 阅读重点 | 预计时间 |
|------|------|---------|---------|
| 5 | `{util_file}` | 工具函数 | {time} min |
| 6 | `{test_file}` | 测试用例，理解预期行为 | {time} min |

---

## 可跳过的文件

以下文件在首次阅读时可安全跳过：

- `{generated_file}` — 自动生成
- `{ci_file}` — CI 配置，非核心逻辑
- `{lock_file}` — 依赖锁定文件

---

## 适合新手的第一个贡献方向

> 基于代码复杂度和测试覆盖率，推荐以下入手点：

1. **{good_first_issue_1}**: {description}
   - 涉及文件: `{file}`
   - 难度: ⭐{difficulty}/5
2. **{good_first_issue_2}**: {description}
   - 涉及文件: `{file}`
   - 难度: ⭐{difficulty}/5
